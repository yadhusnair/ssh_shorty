# Admin tool — distributed separately to admins only, never pushed to the public repo
s-admin
s-admin-watch
s-admin-watch.service
completion-admin.zsh
completion-admin.bash
install-admin.sh

machines.txt
*.log
eventlog.txt

# User registry — contains SSH keys; lives in ~/.config/ssh_shorty/, not the repo
users.txt

# Editor and OS artifacts
*~
.DS_Store
*.swp

# Temp files
*.tmp
*.bak
